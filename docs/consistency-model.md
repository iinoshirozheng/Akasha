# Consistency model

Status: planned.

The first storage engine will use one writer, concurrent snapshot readers, monotonically increasing sequence numbers, and atomic manifest publication. Distributed consistency is outside the first milestone.
